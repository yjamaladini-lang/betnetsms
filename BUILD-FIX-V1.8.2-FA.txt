نوتیفر هرمز — نسخه 1.8.2

رفع خطای GitHub Actions:
MainActivity.java نمی‌توانست BuildConfig.VERSION_NAME را پیدا کند.

اصلاح:
- وابستگی به BuildConfig به‌طور کامل حذف شد.
- نسخه نصب‌شده مستقیماً از Android Package Manager خوانده می‌شود.
- برای Android 13 به بالا از PackageInfoFlags استفاده می‌شود.
- برای نسخه‌های قدیمی‌تر اندروید مسیر سازگار جداگانه حفظ شده است.
- در صورت خطای غیرمنتظره، نسخه 1.8.2 نمایش داده می‌شود.
- تنظیمات AndroidX، Jetifier و اصلاح تداخل Kotlin حفظ شده‌اند.
